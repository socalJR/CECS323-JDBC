#include<iostream>
#include <iomanip>
#include <string>
#include <limits>
#include <ios>
using namespace std;

BigInt GoldRabbit(int n);

ostream& operator<<(ostream& os,const BigInt& dt);

int fact(int n);
